# Copier en backend.hcl (gitignoré) puis : terraform init -backend-config=backend.hcl
# Le bucket est créé par infra/terraform/bootstrap (sortie `state_bucket_name`).
bucket = "thy-production-tfstate-thy-production-XXXXXX"
prefix = "environments/production"
